name: soliplex_packages_workspace
description: Shared packages for Soliplex — currently the Monty interpreter wrapper used by llama_monty.
publish_to: none

environment:
  sdk: ^3.6.0

workspace:
  - packages/soliplex_interpreter_monty

dependency_overrides:
  struct_log:
    git:
      url: https://github.com/runyaga/struct_log.git
