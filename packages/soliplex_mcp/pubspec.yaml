name: soliplex_mcp
description: MCP connection manager wrapping mcp_dart for tool discovery and invocation.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  mcp_dart: ^2.1.0
  meta: ^1.11.0

dev_dependencies:
  mocktail: ^1.0.4
  soliplex_completions:
    path: ../soliplex_completions
  test: ^1.24.0
  very_good_analysis: ^10.0.0
