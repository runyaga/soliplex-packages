name: soliplex_completions
description: LLM provider implementations wrapping Anthropic, OpenAI, and Ollama SDKs.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  anthropic_sdk_dart: ^1.2.1
  http: ^1.2.0
  meta: ^1.11.0
  ollama_dart: ^0.2.0
  open_responses: 0.1.4
  openai_dart: ^1.3.0
  soliplex_client:
    git:
      url: https://github.com/runyaga/soliplex-agent.git
      path: packages/soliplex_client

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.24.0
  very_good_analysis: ^10.0.0
